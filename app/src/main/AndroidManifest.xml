<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="petsnetwork.juanka.android">

    <uses-permission android:name="android.permission.VIBRATE" />

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/dog"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".SearchDogActivity"></activity>
        <activity
            android:name=".EducationDog"
            android:label="@string/title_activity_education_dog"
            android:theme="@style/AppTheme.NoActionBar.NoActionBar" />


        <activity
            android:name="petsnetwork.juanka.AddDogAdopcion"
            android:label="@string/title_activity_add_dog_adopcion"
            android:theme="@style/AppTheme.NoActionBar.NoActionBar" />
        <!--
             <service
            android:name=".services.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        -->
        <activity android:name=".TiendaActivity" />
        <activity android:name=".AdoptionActivity" />
        <activity android:name=".InfoPersonActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity android:name=".PetsLogin"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CrearCuenta"
            android:label="@string/title_activity_crear_cuenta" />
        <activity
            android:name=".PersonProfileActivity"
            android:label="@string/person_profile_activity" />
        <activity android:name=".ComentariosActivity" />
        <activity android:name=".SetupActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>